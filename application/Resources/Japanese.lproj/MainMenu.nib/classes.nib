{
    IBClasses = (
        {CLASS = BSScriptsMenu; LANGUAGE = ObjC; SUPERCLASS = NSMenu; }, 
        {
            ACTIONS = {
                checkForUpdate = id; 
                clearHistory = id; 
                closeAll = id; 
                launchCMLF = id; 
                miniaturizeAll = id; 
                openURL = id; 
                openURLPanel = id; 
                openWebSiteForUpdate = id; 
                resetApplication = id; 
                runBoardWarrior = id; 
                showAcknowledgment = id; 
                showPreferencesPane = id; 
                showStandardFindPanel = id; 
                showTaskInfoPanel = id; 
                showThreadFromHistoryMenu = id; 
                startHEADCheckDirectly = id; 
                toggleOnlineMode = id; 
                togglePreviewPanel = id; 
            }; 
            CLASS = CMRAppDelegate; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            CLASS = CMRDocumentController; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSDocumentController; 
        }, 
        {CLASS = CMRFontManager; LANGUAGE = ObjC; SUPERCLASS = NSFontManager; }, 
        {
            ACTIONS = {
                actionMenuHeader = id; 
                addBoardListItem = id; 
                addCategoryItem = id; 
                addFavorites = id; 
                addSmartItem = id; 
                biggerText = id; 
                cancelCurrentTask = id; 
                changeBrowserArrangement = id; 
                cleanupDatochiFiles = id; 
                clearMessageAttributes = id; 
                collapseOrExpandBoardList = id; 
                collapseOrExpandThreadViewer = id; 
                copySelectedResURL = id; 
                copyThreadAttributes = id; 
                deleteThread = id; 
                editBoardListItem = id; 
                findAll = id; 
                findAllByFilter = id; 
                findFirstText = id; 
                findNextText = id; 
                findPreviousText = id; 
                findTextInSelection = id; 
                googleSearch = id; 
                historyMenuPerformBack = id; 
                historyMenuPerformForward = id; 
                openBBSInBrowser = id; 
                openInBrowser = id; 
                openScriptsDirectory = id; 
                pasteAsQuotation = id; 
                reloadThread = id; 
                reloadThreadsList = id; 
                removeBoardListItem = id; 
                reply = id; 
                revealInFinder = id; 
                runAsciiArtDetector = id; 
                runBoardWarrior = id; 
                runSpamFilter = id; 
                saveAsDefaultFrame = id; 
                scrollFirstMessage = id; 
                scrollLastMessage = id; 
                scrollNextBookmark = id; 
                scrollNextMessage = id; 
                scrollPrevMessage = id; 
                scrollPreviousBookmark = id; 
                scrollToFirstTodayMessage = id; 
                scrollToLastReadedIndex = id; 
                scrollToLastUpdatedIndex = id; 
                selectFilteringMask = id; 
                setOnMessageAttributes = id; 
                showBoardInspectorPanel = id; 
                showDocumentInfo = id; 
                showMainBrowser = id; 
                showMessageMatchesAttributes = id; 
                showSearchThreadPanel = id; 
                showStandardFindPanel = id; 
                smallerText = id; 
                toggleAAThread = id; 
                toggleDatOchiThread = id; 
                toggleMarkedThread = id; 
            }; 
            CLASS = FirstResponder; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {openScriptsDirectory = id; }; 
            CLASS = NSApplication; 
            LANGUAGE = ObjC; 
            SUPERCLASS = NSResponder; 
        }, 
        {CLASS = NSDocumentController; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {CLASS = NSWindow; LANGUAGE = ObjC; SUPERCLASS = NSResponder; }
    ); 
    IBVersion = 1; 
}